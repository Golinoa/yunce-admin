# Admin 工程化 — Phase 3 完成清单

- [x] `verify:scope`（`scripts/change-scope.mjs`）按 diff 桶推荐最小检查
- [x] PRE-PUSH 接入 scope 命令
- [x] 运营列表起步壳 `useOperationListState` + 单测
- [x] 覆盖率硬门槛：仍延后（本地 registry 不稳）；ADR-0002 已约定升门槛流程

工程化主轨道 Phase 0–3 完成；随后跑全量 `verify:sop`（含 build）作 QA。
